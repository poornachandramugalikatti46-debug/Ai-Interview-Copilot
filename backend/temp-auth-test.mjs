import { registerUser } from './controllers/authController.js'; const req = { body: { fullname: 'Test', email: 'x4@example.com', password: 'secret' } }; const res = { statusCode: 200, status(c) { this.statusCode = c; return this; }, json(body) { console.log(JSON.stringify(body)); return body; } }; try { await registerUser(req, res); } catch (err) { console.error(err && err.stack || err); process.exit(1); }
